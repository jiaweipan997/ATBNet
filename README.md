# ATBNet
# This repo is the official implementation of "ATBNet". This repo contains the supported code and configuration files to reproduce the semantic segmentation results of "ATBNet". It is based on mmsegmentaion. The code will be organized and uploaded after the paper is accepted.
